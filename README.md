# GraphQLCRUDDemo
In the .NET ecosystem, GraphQL is typically implemented using libraries like Hot Chocolate, which integrate seamlessly with ASP.NET Core and Entity Framework. These libraries support key GraphQL features such as queries (read operations), mutations (write operations), subscriptions (real-time updates), filtering, sorting and data loading.
